<%= form_for(@payee) do |f| %>
  <% if @payee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payee.errors.count, "error") %> prohibited this payee from being saved:</h2>

      <ul>
      <% @payee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :recurring %><br>
    <%= f.select :recurring, options_for_select(%w[annually monthly weekly]) %>
  </div>
  <% if @user %>
    <%= hidden_field_tag :user_id, @user.id %>
  <% end %>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
